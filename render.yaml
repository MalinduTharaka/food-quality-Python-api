services:
  - type: web
    name: food-quality-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python food_quality_api.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
